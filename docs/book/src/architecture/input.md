# Input Handling

<!-- TODO: expand with keyboard/gamepad mapping details and edge detection -->

Raven abstracts keyboard and gamepad input behind a unified `InputState` struct with normalised axes and edge-detected button presses.

Key files: `src/core/input.hpp`, `src/ecs/systems/input_system.hpp`.
